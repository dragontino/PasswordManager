package com.security.passwordmanager.presentation.view.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontLoadingStrategy
import com.security.passwordmanager.R

val RobotoFont = Font(resId = R.font.roboto)
val VerdanaFont = Font(resId = R.font.verdana)
val BeautifulFont = Font(
    resId = R.font.beautiful_font,
    loadingStrategy = FontLoadingStrategy.Async
)
val PacificFont = Font(
    resId = R.font.pacifico,
    loadingStrategy = FontLoadingStrategy.Async
)